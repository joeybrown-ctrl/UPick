//event creation process + MVP activity providers (APIs)

//Contract: the backend will adhere to a contract of what the front end is expecting.
//What POST requests does the FE need?

//When the FE wants to create an event, what endpoint are they going to hit? (endpoint: name of the route... so
// like "/api", "/event", etc)

